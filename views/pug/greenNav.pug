nav.navbar.navbar-expand-sm.navbar-dark.bg-dark.sticky-top.p-0
	// One of the primary actions on mobile is to call a business - This displays a phone button on mobile only
	.navbar-toggler-right
		button.navbar-toggler(type='button' data-toggle='collapse' data-target='#navbar')
			span.navbar-toggler-icon
	#greenNav.collapse.navbar-collapse.row
		ul.col-6.navbar-nav.pe-0.p-1
			each name, i in fora.categories.names
				li.flex-sm-fill.text-sm-center.nav-item.dropdown
					a.nav-link(role='button' data-bs-toggle='dropdown')= fora.categories.names[i].toUpperCase()
					ul.dropdown-menu.container.p-2(id=`${name}Menu`): .row.mb-4
							.col-8.px-4
								.row
									a.col-12.fs-4.green(href='#' onclick='fetchItems(' + (i+1) +')')= 'Shop ' + name[0].toUpperCase() + name.slice(1)
									.col  _____________ 
									.col  _____________ 
									.col  _____________ 
							.col-4.images
		.col-4.navbar-nav
		ul.col-2.navbar-nav.pe-0
			// about us list
			li.flex-sm-fill.text-sm-center
				a.nav-link(role='button' href='/about') ABOUT US